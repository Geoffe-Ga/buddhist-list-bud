[build]
command = "pip install -r backend/requirements.txt && cd frontend && npm install && npm run build"

[deploy]
startCommand = "python -m uvicorn backend.app.main:app --host 0.0.0.0 --port ${PORT:-8000}"
